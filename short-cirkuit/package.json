{
  "name": "short-cirkuit-reskin",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "lint:frontend": "npm run lint --workspace frontend"
  }
}
